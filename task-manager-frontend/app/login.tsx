// app/register-form.tsx (or pages/register-form.tsx) // Adjust the import if needed
import LoginForm from "./components/Auth/LoginForm";

const Login = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Login;
