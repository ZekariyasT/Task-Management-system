// app/register-form.tsx (or pages/register-form.tsx)
import RegisterForm from "@/app/components/Auth/RegisterForm"; // Adjust the import if needed

const Register = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Register;
